.page-header
%table.table.table-striped
  %thead
    %tr
      %th ID
      %th name
      %th status
  %tbody
    - @events.each do |event|
      %tr
        %td= event.id
        %td= link_to event.name, event_path(event)
        %td= event.status
