.page-header
%table.table.table-striped
  %thead
    %tr
      %th ID
      %th name
      %th description
      %th status
  %tbody
    - @events.each do |event|
      %tr
        %td= event.id
        %td= event.name
        %td= event.description
